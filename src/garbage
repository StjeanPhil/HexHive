countConnectedBugs(alreadyVisited, x, y, tempGrid){

bugs = 0;

bugs += tempGrid[x][y].content.length;

alreadyVisited.append([x, y]);

if (voisin1 not in alreadyVisited){
newBugs, updatedAlreadyVisited = countConnectedBugs((alreadyVisited, newX, newY, tempGrid);
bugs += newBugs
alreadyVisited = updatedAlreadyVisited
}

if (voisin2 not in alreadyVisited){
newBugs, updatedAlreadyVisited = countConnectedBugs((alreadyVisited, newX, newY, tempGrid);
bugs += newBugs
alreadyVisited = updatedAlreadyVisited
}

...

if (voisin8 not in alreadyVisited){
newBugs, updatedAlreadyVisited = countConnectedBugs((alreadyVisited, newX, newY, tempGrid);
bugs += newBugs
alreadyVisited = updatedAlreadyVisited
}

return bugs, alreadyVisited;
}